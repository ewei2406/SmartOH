import Logout from "../Components/Logout"

const RoomTAView = ({ currentData, setCurrentData }: any) => {
    return <div>
        hello world TA view
        <Logout setCurrentData={setCurrentData}/>
    </div>
}

export default RoomTAView